# Example .env for PostgreSQL
# Update the values as needed for your environment

# Database URL format for async SQLAlchemy with asyncpg driver
DATABASE_URL=postgresql://myuser:mypassword@localhost:5432/mydatabase

# Example values:
#   myuser:      your PostgreSQL username
#   mypassword:  your PostgreSQL password
#   localhost:   your DB host (use a remote IP or Docker service name if needed)
#   5432:        your PostgreSQL port (default is 5432)
#   mydatabase:  your database name

# Other config values...
OPENAI_API_KEY=your-openai-key
TELEGRAM_BOT_TOKEN=your-telegram-token
WEBHOOK_SECRET=your-webhook-secret
